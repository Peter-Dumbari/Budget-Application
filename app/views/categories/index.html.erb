<div class="container-fluid mt-5 ">
    <h3 class="text-center fw-bold">LIST OF CATEGORIES</h3>
    <div class="row">
        <div class="col-6 offset-3  p-4 bg-light">
            <div class="mt-2 ">
                    <% @categories.each do |category| %>
                        <%= render partial: 'shared/categories_card', locals: { category: category } %>
                    <% end %>
            </div>
            <div class="">
            <a href=<%= new_category_path %> class="btn btn-info rounded-0 w-100 p-3 text-decoration-none fw-bold text-white">Add Category</a>
        </div>
        </div>
    </div> 
</div>